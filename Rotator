using UnityEngine;
using System.Collections;

public class Rotator : MonoBehaviour
{
    private const int X = 15;

    // Start is called before the first frame update
    void Update()
    {
        transform.Rotate (new Vector3 (X, 30, 45) * Time.deltaTime);
    }
}
